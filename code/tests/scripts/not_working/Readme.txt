This directory is for the test scripts that are not working but are a part of the Earlham-Smoke project.

non_printable_opt.sh and unknown_opt.sh print using cerr need a way to catch that.

log_default.sh doesn't seem to let us have the -l option without a specific file name.
